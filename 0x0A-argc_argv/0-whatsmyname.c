#include <stdlib.h>
#include <stdio.h>
/**
 * main - prints the name of the programme
 * @argc: number of argument
 * @argv: an array contain the argument
 * Return: 0
 */
int main(__attribute__((unused))int argc, char *argv[])
{
	printf("%s\n",argv[0]);
	return(0);
}
